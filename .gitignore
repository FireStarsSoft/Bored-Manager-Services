node_modules/
dist/
# esbuild output for the module's main half. Rebuilt on demand by the app that
# installs it, deliberately kept out of the release zip, and skipped by the
# folder hash - so it is never source.
service-fleet/.dist/
*.log
.claude/
.cursor/

# Working notes for whatever is being done right now: local scratch for whoever
# is doing it, not source and not documentation. The same rule the OpenWRT repo
# applies to its own Plan.MD.
Plan.MD

# Python build and test artefacts from the agent.
__pycache__/
*.py[cod]
agent/venv/
agent/.venv/
agent/.pytest_cache/
agent/*.egg-info/
dist-agent/
